pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                // build docker
                echo "hello12345679"
                    // docker build \
                    // -t "morse:v1" \
                    // --target "server" \
                    // -f Dockerfile \
                    // .
                // To run Maven on a Windows agent, use
            }
        }
    }
}
