pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                // build docker
                echo "123334"
                    // docker build \
                    // -t "morse:v1" \
                    // --target "server" \
                    // -f Dockerfile \
                    // .

                // To run Maven on a Windows agent, use
                // bat "mvn -Dmaven.test.failure.ignore=true clean package"
            }


        }

    }
}
